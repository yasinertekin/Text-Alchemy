part of '../image_picker_view.dart';

final class _TextIsEmpty extends StatelessWidget {
  const _TextIsEmpty();

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(StringConstants.textIsEmpty),
    );
  }
}
