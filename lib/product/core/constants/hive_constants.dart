import 'package:flutter/material.dart';

@immutable

/// HiveConstants
final class HiveConstants {
  const HiveConstants._();

  /// Hive box name
  static const String hiveBoxName = 'pinned';

  /// Hive type id
  static const int hiveTypeId = 0;
}
